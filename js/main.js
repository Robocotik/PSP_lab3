import {MainPage} from '../pages/main/index.js';

const root = document.getElementById('root');

const mainPage = new MainPage(root);
// const subjeectPage = new SubjectPage(root);
mainPage.render();
// subjeectPage.render(slides[0]);
